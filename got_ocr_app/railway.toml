[build]
builder = "nixpacks"

[deploy]
startCommand = "uvicorn main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 300        # generous — model takes time to load on cold start
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
